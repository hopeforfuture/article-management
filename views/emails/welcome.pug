doctype html
html
  head
    meta(charset="UTF-8")
    title Welcome to Our App
    style.
      body {
        font-family: Arial, sans-serif;
        background-color: #f9f9f9;
        padding: 20px;
        color: #333;
      }
      .container {
        background: #fff;
        padding: 30px;
        border-radius: 10px;
        max-width: 600px;
        margin: auto;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      }
      .btn {
        display: inline-block;
        margin-top: 20px;
        padding: 12px 20px;
        background-color: #007bff;
        color: #fff;
        text-decoration: none;
        border-radius: 6px;
      }
      .btn:hover {
        background-color: #0056b3;
      }
  body
    .container
      h1 Welcome, #{name}!
      p Thank you for signing up at <b>#{appName}</b>. 
      p To complete your registration, please verify your email by clicking the button below:
      a.btn(href=url target="_blank") Verify My Account
      p If the button doesn’t work, copy and paste this link into your browser:
      p #{url}
      hr
      p(style="font-size: 12px; color: #777;")
        If you didn’t create an account, you can safely ignore this email.
